const paths={network:'M6 7l6 5 6-5M12 12v7M6 7a2 2 0 1 0 0 .01M18 7a2 2 0 1 0 0 .01M12 19a2 2 0 1 0 0 .01',grid:'M3 3h7v7H3zM14 3h7v7h-7zM3 14h7v7H3zM14 14h7v7h-7z',tree:'M5 4v13h13M5 8h13M18 5v6M18 14v6',search:'M10 17a7 7 0 1 0 0-14 7 7 0 0 0 0 14m5-2 6 6',arrow:'M5 12h14m-5-5 5 5-5 5',chevron:'m9 5 7 7-7 7',down:'m5 9 7 7 7-7',close:'m6 6 12 12M6 18 18 6',lock:'M6 10h12v10H6zM8 10V6a4 4 0 0 1 8 0v4',book:'M3 4h7l2 2 2-2h7v15h-7l-2 2-2-2H3zM12 6v15',filter:'M4 6h16M7 12h10M10 18h4',layers:'m12 3 9 5-9 5-9-5 9-5m-9 9 9 5 9-5M3 16l9 5 9-5',bookmark:'M6 3h12v18l-6-4-6 4z',help:'M12 21a9 9 0 1 0 0-18 9 9 0 0 0 0 18m-3-12a3 3 0 0 1 6 0c0 2-3 2-3 4m0 4v.1',external:'M14 3h7v7m0-7-11 11M10 3H3v18h18v-7',link:'m10 14 4-4M8 16l-2 2a4 4 0 0 1-6-6l5-5a4 4 0 0 1 6 0m2 1 2-2a4 4 0 0 1 6 6l-5 5a4 4 0 0 1-6 0',list:'M8 6h13M8 12h13M8 18h13M3 6h.1M3 12h.1M3 18h.1',spark:'m12 3 2.5 6.5L21 12l-6.5 2.5L12 21l-2.5-6.5L3 12l6.5-2.5L12 3',logic:'M4 5h5v5H4zM15 14h5v5h-5zM9 7h8v7M7 10v7h8',leaf:'M20 3C7 2 1 10 6 17s15 1 14-14M5 20 16 8',neural:'M5 5a2 2 0 1 0 0 .1M5 19a2 2 0 1 0 0 .1M12 12a2 2 0 1 0 0 .1M19 5a2 2 0 1 0 0 .1M19 19a2 2 0 1 0 0 .1M7 6l3 4m4 0 3-4M7 18l3-4m4 0 3 4',text:'M4 5h16M12 5v15M8 20h8',eye:'M2 12s4-7 10-7 10 7 10 7-4 7-10 7-10-7-10-7m10 3a3 3 0 1 0 0-6 3 3 0 0 0 0 6',bot:'M5 7h14v13H5zM12 7V3M9 12h.1M15 12h.1M9 16h6M2 10v7M22 10v7',database:'M4 6c0-5 16-5 16 0s-16 5-16 0m0 0v12c0 5 16 5 16 0V6M4 12c0 5 16 5 16 0',server:'M3 3h18v7H3zM3 14h18v7H3zM7 6h.1M7 17h.1M11 6h6M11 17h6',shield:'M12 3 3 6v6c0 6 9 10 9 10s9-4 9-10V6l-9-3m-4 9 3 3 5-6',plus:'M12 5v14M5 12h14',minus:'M5 12h14',fit:'M9 3H3v6M15 3h6v6M3 15v6h6M21 15v6h-6',menu:'M3 6h18M3 12h18M3 18h18',check:'m5 12 4 4L19 6',warning:'m12 3 10 18H2L12 3m0 5v6m0 3v.1',compass:'M12 22a10 10 0 1 0 0-20 10 10 0 0 0 0 20m4-16-3 7-7 3 3-7 7-3',clock:'M12 21a9 9 0 1 0 0-18 9 9 0 0 0 0 18m0-14v5l4 3'};
const domainIcons={ai:'network',symbolic:'logic',ml:'leaf',dl:'neural',nlp:'text',vision:'eye',gen:'spark',agents:'bot',data:'database',ops:'server',trust:'shield'};
const icon=(name,cls='')=>`<svg class="icon ${cls}" viewBox="0 0 24 24" aria-hidden="true"><path d="${paths[name]||paths.network}"/></svg>`;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const norm=s=>String(s).normalize('NFD').replace(/\p{Diacritic}/gu,'').toLowerCase();
const app=document.getElementById('app');
let data,byId,children,domains,searchIndex;
const route=new URLSearchParams(location.hash.slice(1));
let favorites=[];try{favorites=JSON.parse(localStorage.getItem('atlas-ia-favorites-v1')||'[]');if(!Array.isArray(favorites))favorites=[]}catch{}
const state={selected:route.get('concept')||'ai',view:['tree','graph','index'].includes(route.get('view'))?route.get('view'):'tree',q:'',kind:'',domain:'',level:'',expanded:new Set(),drawer:null,favorites:new Set(favorites),savedOnly:false,menu:false,depth:1,enabled:new Set(['hierarchy','uses','enables','evaluates','controls','risk']),page:1,zoom:1,pan:{x:0,y:0}};
let searchTimer,toastTimer,drawerReturn;
try{const r=await fetch('./data.json');if(!r.ok)throw Error(`HTTP ${r.status}`);data=await r.json();byId=new Map(data.nodes.map(n=>[n.id,n]));children=new Map(data.nodes.map(n=>[n.id,data.nodes.filter(x=>x.parent===n.id)]));domains=children.get('ai');searchIndex=new Map(data.nodes.map(n=>[n.id,norm([n.label,n.summary,n.use,...n.tags].join(' '))]));if(!byId.has(state.selected))state.selected='ai';for(const id of ancestors(state.selected))state.expanded.add(id);state.favorites=new Set([...state.favorites].filter(id=>byId.has(id)));render();}catch(err){console.error(err);app.innerHTML=`<div class="empty"><h1>L’atlas n’a pas pu être chargé.</h1><p>Vérifiez votre connexion, puis rechargez la page.</p><button class="btn" onclick="location.reload()">Réessayer</button></div>`;}
function ancestors(id){const ids=[];let n=byId.get(id);while(n){ids.unshift(n.id);n=byId.get(n.parent)}return ids}
function descendants(id){return data.nodes.filter(n=>n.id!==id&&ancestors(n.id).includes(id))}
function relevantEdges(id){return data.edges.filter(e=>e.from===id||e.to===id)}
function updateRoute(push=false){const params=new URLSearchParams({concept:state.selected,view:state.view});history[push?'pushState':'replaceState'](null,'','#'+params);}
function announce(s){document.getElementById('announcement').textContent=s}
function toast(s){const el=document.getElementById('toast');el.textContent=s;el.classList.add('visible');clearTimeout(toastTimer);toastTimer=setTimeout(()=>el.classList.remove('visible'),2800)}
function navigate(id,view){clearTimeout(searchTimer);if(!byId.has(id))return;state.selected=id;if(view)state.view=view;state.q='';state.kind='';state.domain='';state.level='';state.savedOnly=false;state.page=1;state.drawer=null;state.menu=false;state.zoom=1;state.pan={x:0,y:0};for(const ancestor of ancestors(id))state.expanded.add(ancestor);updateRoute(true);render();announce(`${byId.get(id).label}, niveau ${byId.get(id).level}.`);document.getElementById('main-title')?.focus({preventScroll:true});}
function options(items,value){return items.map(([v,l])=>`<option value="${esc(v)}" ${v===value?'selected':''}>${esc(l)}</option>`).join('')}
function treeBranch(id,depth=0){return (children.get(id)||[]).map(n=>{const has=children.get(n.id).length>0;return `<div class="tree-row ${state.selected===n.id?'selected':''}" style="--node:${n.color}"><button class="tree-select" data-action="navigate" data-id="${n.id}" ${state.selected===n.id?'aria-current="page"':''} title="${esc(n.label)}"><span class="tree-dot"></span><span class="tree-label">${esc(n.label)}</span></button>${has?`<button class="tree-toggle" data-action="expand" data-id="${n.id}" aria-label="${state.expanded.has(n.id)?'Replier':'Déplier'} ${esc(n.label)}" aria-expanded="${state.expanded.has(n.id)}">${icon(state.expanded.has(n.id)?'down':'chevron')}</button>`:'<span style="width:27px"></span>'}</div>${has&&state.expanded.has(n.id)?`<div class="tree-indent">${treeBranch(n.id,depth+1)}</div>`:''}`}).join('')}
function render(){if(!data)return;const focused=document.activeElement;const focusId=focused?.id;const selection=focused?.selectionStart;const treeScroll=document.getElementById('tree')?.scrollTop||0;const n=byId.get(state.selected);const isRoot=n.id==='ai';const activeFilter=state.q||state.kind||state.domain||state.level||state.savedOnly;
app.innerHTML=`<div class="layout ${state.menu?'menu-open':''}" ${state.drawer?'inert':''}>
${state.menu?'<button class="menu-backdrop" data-action="menu-close" aria-label="Fermer la navigation"></button>':''}
<aside class="sidebar" aria-label="Navigation de l’atlas">
<div class="brand"><div class="brand-mark">${icon('network')}</div><div><div class="brand-name">ATLAS</div><small>INTELLIGENCE ARTIFICIELLE</small></div></div>
<div class="sidebar-top"><button class="sidebar-nav ${state.selected==='ai'&&!state.savedOnly?'active':''}" data-action="navigate" data-id="ai">${icon('compass')}Vue d’ensemble</button><button class="sidebar-nav ${state.savedOnly?'active':''}" data-action="favorites">${icon('bookmark')}Mes repères<span class="badge">${state.favorites.size}</span></button></div>
<div class="nav-label">LES DOMAINES <span>10</span></div><nav id="tree" class="tree" aria-label="Arborescence des concepts">${treeBranch('ai')}</nav>
<div class="sidebar-bottom"><div class="avatar">AE</div><div><strong>Atlas de connaissances</strong><small>Explorer · Comprendre · Relier</small></div>${icon('compass')}</div></aside>
<div class="main-area"><header class="topbar"><button class="mobile-menu" data-action="menu" aria-label="Ouvrir la navigation" aria-expanded="${state.menu}">${icon('menu')}</button><div class="topbar-title"><b>Bibliothèque de connaissances</b><span class="topbar-divider">/</span>Atlas IA</div>
<div class="searchbox">${icon('search')}<label for="search" class="sr-only">Rechercher un concept, une méthode ou une technologie</label><input id="search" type="search" value="${esc(state.q)}" placeholder="Rechercher un concept, une technologie…" autocomplete="off" spellcheck="false">${state.q?'<button class="search-clear" data-action="clear-search" aria-label="Effacer la recherche">'+icon('close')+'</button>':'<kbd aria-hidden="true">/</kbd>'}</div>
<button class="topbar-btn" data-action="help" aria-label="Aide et raccourcis">${icon('help')}</button><div class="access-pill" title="Cartographie publique hébergée sur GitHub Pages">${icon('network')}Public</div></header>
<main id="main" class="main"><nav class="breadcrumb" aria-label="Fil d’Ariane">${ancestors(n.id).map((id,i,arr)=>`${i?icon('chevron'):''}<button data-action="navigate" data-id="${id}" class="${i===arr.length-1?'current':''}" ${i===arr.length-1?'aria-current="page"':''}>${esc(id==='ai'?'Paysage de l’IA':byId.get(id).label)}</button>`).join('')}</nav>
<div class="hero"><div><div class="eyebrow">${isRoot?'Une vue connectée de l’intelligence artificielle':`${esc(n.kind)} · Niveau ${n.level}`}</div><h1 id="main-title" tabindex="-1">${isRoot?'Le paysage de l’IA.<br><em>Ses connexions, en clair.</em>':esc(n.label)}</h1><p class="hero-text">${isRoot?'Du raisonnement symbolique aux agents autonomes. Explorez les domaines, leurs technologies et les liens qui les unissent.':esc(n.summary)}</p></div><div class="hero-actions">${isRoot?`<button class="btn primary" data-action="navigate-graph" data-id="rag">Explorer le RAG ${icon('arrow')}</button>`:`<button class="btn primary" data-action="details" data-id="${n.id}">${icon('book')}Fiche du concept</button>`}<button class="btn" data-action="sources">${icon('book')}Sources</button></div></div>
<div class="stats" aria-label="Chiffres de la cartographie">${stat('grid',domains.length,'domaines')}${stat('network',data.nodes.length-1,'concepts')}${stat('link',data.edges.length,'relations')}${stat('layers',Math.max(...data.nodes.map(x=>x.level)),'niveaux')}</div>
<section class="workspace" aria-label="Exploration interactive"><div class="workspace-head"><div class="tabs" role="tablist" aria-label="Mode d’exploration">${[['tree','Arborescence','tree'],['graph','Graphe','network'],['index','Index','list']].map(([v,l,i])=>`<button class="tab ${state.view===v?'active':''}" role="tab" aria-selected="${state.view===v}" id="tab-${v}" aria-controls="exploration-content" data-action="view" data-view="${v}">${icon(i)}${l}</button>`).join('')}</div><div class="scope-label"><span class="live-dot"></span>${state.q?'Recherche dans tout l’atlas':state.savedOnly?'Vos concepts enregistrés':isRoot?'Tous les domaines':esc(n.label)}</div></div>
<div class="filters"><span class="filter-label">${icon('filter')}Filtrer</span><label class="sr-only" for="domain-filter">Filtrer par domaine</label><select id="domain-filter" data-filter="domain">${options([['','Tous les domaines'],...domains.map(x=>[x.id,x.label])],state.domain)}</select><label class="sr-only" for="kind-filter">Filtrer par type de concept</label><select id="kind-filter" data-filter="kind">${options([['','Tous les types'],...['domaine','sous-domaine','méthode','approche','architecture','technologie','risque','référentiel'].map(x=>[x,x.charAt(0).toUpperCase()+x.slice(1)])],state.kind)}</select><label class="sr-only" for="level-filter">Filtrer par niveau</label><select id="level-filter" data-filter="level">${options([['','Tous les niveaux'],...Array.from({length:4},(_,i)=>[String(i+1),`Niveau ${i+1}`])],state.level)}</select>${activeFilter?'<button class="filter-reset" data-action="reset">Réinitialiser</button>':''}<span class="filter-count" id="filter-count"></span></div>
<div id="exploration-content" role="tabpanel" aria-labelledby="tab-${state.view}">${state.view==='graph'&&!state.q&&!state.savedOnly?renderGraph():renderResults()}</div>
<div class="workspace-foot"><span>${state.view==='graph'?'Sélectionnez un nœud pour recentrer le graphe.':state.q?'Recherche sur les noms, synonymes, descriptions et usages.':'Sélectionnez un concept pour descendre d’un niveau.'}</span><button data-action="legend">${icon('layers')}Lire la légende</button></div></section>
${isRoot&&!activeFilter?`<div class="path-banner">${icon('spark')}<div><strong>Un fil pour commencer : du modèle à l’agent documentaire</strong><p>LLM → RAG → recherche → outils → supervision humaine.</p></div><button data-action="navigate-graph" data-id="rag" aria-label="Suivre le parcours du modèle à l’agent documentaire">Suivre les connexions ${icon('arrow')}</button></div>`:''}
</main><footer class="footer"><span>ATLAS IA · Références consultées le 15 septembre 2026</span><div class="footer-links"><button data-action="methodology">Périmètre & méthode</button><button data-action="help">Guide d’exploration</button></div></footer></div></div>${state.drawer?renderDrawer():''}`;
document.body.style.overflow=state.drawer?'hidden':'';
if(document.getElementById('tree'))document.getElementById('tree').scrollTop=treeScroll;
if(focusId&&document.getElementById(focusId)&&!state.drawer){const el=document.getElementById(focusId);el.focus({preventScroll:true});if(typeof selection==='number'&&el.setSelectionRange)try{el.setSelectionRange(selection,selection)}catch{}}
if(state.drawer)document.getElementById('drawer-close').focus({preventScroll:true});
document.getElementById('filter-count').textContent=state.view==='graph'&&!state.q&&!state.savedOnly?`${graphData().nodes.length} nœuds affichés`:`${filteredNodes().length} ${state.savedOnly?'repères':'résultats'}`;
}
function stat(i,value,label){return `<div class="stat"><div class="stat-icon">${icon(i)}</div><div><strong>${value}</strong><span>${label}</span></div></div>`}
function matches(n){return (!state.domain||n.domain===state.domain)&&(!state.kind||n.kind===state.kind)&&(!state.level||n.level===Number(state.level))&&(!state.savedOnly||state.favorites.has(n.id))&&(!state.q||norm(state.q).trim().split(/\s+/).every(q=>searchIndex.get(n.id).includes(q)))}
function filteredNodes(){let pool;if(state.q||state.savedOnly)pool=data.nodes.filter(n=>n.id!=='ai');else if(state.view==='index'||state.domain||state.kind||state.level)pool=descendants(state.selected);else pool=children.get(state.selected);return pool.filter(matches).sort((a,b)=>state.q?score(b)-score(a):a.level-b.level||a.label.localeCompare(b.label,'fr'))}
function score(n){const q=norm(state.q).trim();return norm(n.label)===q?10:n.tags.some(x=>norm(x)===q)?9:norm(n.label).includes(q)?6:1}
function card(n){const count=children.get(n.id).length;const related=relevantEdges(n.id).filter(x=>x.type!=='hierarchy').length;const tags=(count?children.get(n.id).map(x=>x.label):n.tags).slice(0,3);return `<article class="concept-card" style="--node:${n.color}"><button class="card-main" data-action="navigate" data-id="${n.id}" aria-label="Explorer ${esc(n.label)}"><span class="card-arrow">${icon('arrow')}</span><div class="card-heading"><div class="domain-icon">${icon(domainIcons[n.domain])}</div><div><h3 class="card-title">${esc(n.label)}</h3><div class="card-kicker">${esc(n.kind)} · Niveau ${n.level}</div></div></div><p class="card-description">${esc(n.summary)}</p>${tags.length?`<div class="card-tags">${tags.map(t=>`<span class="mini-tag">${esc(t)}</span>`).join('')}${count>3?`<span class="mini-tag">+${count-3}</span>`:''}</div>`:''}</button><div class="card-footer"><small>${icon(count?'layers':'link')}${count?`${count} entrées`:`${related} liens transverses`}</small><button class="card-details" data-action="details" data-id="${n.id}" aria-label="Ouvrir la fiche de ${esc(n.label)}">Fiche synthétique ${icon('external')}</button></div></article>`}
function renderResults(){const list=filteredNodes();const n=byId.get(state.selected);const pageSize=state.view==='index'?35:24;const pages=Math.ceil(list.length/pageSize);state.page=Math.max(1,Math.min(state.page,pages||1));const visible=list.slice((state.page-1)*pageSize,state.page*pageSize);let title=state.q?`Résultats pour « ${state.q} »`:state.savedOnly?'Mes repères':n.id==='ai'?(state.kind||state.domain||state.level||state.view==='index'?'Tous les concepts':'Les dix domaines'):children.get(n.id).length?'Explorer ce niveau':'Concepts liés';
let html=`<div class="results-heading"><h2>${esc(title)}</h2><small>${state.q?'Tout l’atlas':state.savedOnly?'Enregistrés sur cet appareil':`Niveau ${Math.min(n.level+1,4)} et connexions`}</small></div>`;
if(!list.length&&!state.q&&!state.savedOnly&&!state.domain&&!state.kind&&!state.level&&n.id!=='ai'){
 const linked=[...new Set(relevantEdges(n.id).filter(e=>e.type!=='hierarchy').map(e=>e.from===n.id?e.to:e.from))].map(id=>byId.get(id));
 html+=`<div class="concept-intro" style="--node:${n.color}"><div class="domain-icon intro-icon">${icon(domainIcons[n.domain])}</div><div class="intro-text"><h2>Vous avez atteint une fiche de détail.</h2><p>Explorez maintenant ses connexions ou ouvrez sa fiche pour lire les usages, limites et références.</p></div><button class="btn" data-action="details" data-id="${n.id}">${icon('book')}Lire la fiche</button></div>`;
 if(linked.length)html+=`<div class="cards">${linked.map(card).join('')}</div>`;else html+=`<div class="empty"><p>Ce concept n’a pas de lien transverse dans cette version. Remontez au niveau parent pour explorer les autres méthodes.</p><button class="btn" data-action="navigate" data-id="${n.parent}">Niveau parent ${icon('arrow')}</button></div>`;
 return html;
}
if(!list.length)return html+`<div class="empty">${icon(state.savedOnly?'bookmark':'search')}<h2>${state.savedOnly?'Vos repères commencent ici.':'Aucun concept ne correspond.'}</h2><p>${state.savedOnly?'Ouvrez une fiche et utilisez le signet pour retrouver vos concepts sur cet appareil.':'Essayez un synonyme, un acronyme ou élargissez les filtres.'}</p><button class="btn" data-action="reset">${state.savedOnly?'Explorer tous les domaines':'Effacer la recherche et les filtres'}</button></div>`;
html+=state.view==='index'?`<div class="index-list">${visible.map(x=>`<div class="index-row" style="--node:${x.color}"><div class="domain-icon">${icon(domainIcons[x.domain])}</div><button class="index-info" data-action="navigate" data-id="${x.id}"><strong>${esc(x.label)}</strong><span>${esc(byId.get(x.domain).label)} · Niveau ${x.level}</span></button><span class="index-type">${esc(x.kind)}</span><button class="card-details" data-action="details" data-id="${x.id}" aria-label="Ouvrir la fiche de ${esc(x.label)}">Fiche ${icon('arrow')}</button></div>`).join('')}</div>`:`<div class="cards">${visible.map(card).join('')}</div>`;
if(pages>1)html+=`<div class="pagination"><button class="btn" data-action="page" data-step="-1" ${state.page===1?'disabled':''}>Précédent</button><span>Page ${state.page} / ${pages} · ${list.length} concepts</span><button class="btn" data-action="page" data-step="1" ${state.page===pages?'disabled':''}>Suivant</button></div>`;return html;}
function graphData(){const center=byId.get(state.selected);const enabledEdges=data.edges.filter(e=>state.enabled.has(e.type));const direct=enabledEdges.filter(e=>e.from===center.id||e.to===center.id);const directIds=[...new Set(direct.map(e=>e.from===center.id?e.to:e.from))].filter(id=>matches(byId.get(id)));const chosen=directIds.slice(0,16);const first=new Set(chosen);let omitted=Math.max(0,directIds.length-16);if(state.depth===2){const second=[...new Set(enabledEdges.filter(e=>chosen.includes(e.from)||chosen.includes(e.to)).flatMap(e=>[e.from,e.to]))].filter(id=>id!==center.id&&!first.has(id)&&matches(byId.get(id)));chosen.push(...second.slice(0,12));omitted+=Math.max(0,second.length-12)}const ids=new Set([center.id,...chosen]);const nodes=[center,...chosen.map(id=>byId.get(id))];const edges=enabledEdges.filter(e=>ids.has(e.from)&&ids.has(e.to));return {nodes,edges,first,omitted,totalDirect:directIds.length,allDirect:direct}}
function renderGraph(){const gd=graphData();const wide=state.depth===2,mobile=innerWidth<=600;const w=mobile?380:wide?1500:1200,h=mobile?Math.max(470,135+Math.ceil((gd.nodes.length-1)/2)*88):wide?1050:790,cx=w/2,cy=mobile?48:h/2;const positions=new Map([[state.selected,{x:cx,y:cy}]]);const first=gd.nodes.filter(n=>gd.first.has(n.id));const second=gd.nodes.filter(n=>n.id!==state.selected&&!gd.first.has(n.id));
 function arrange(nodes,rx,ry,offset){nodes.forEach((n,i)=>{const a=-Math.PI/2+i*2*Math.PI/nodes.length+offset;positions.set(n.id,{x:cx+Math.cos(a)*rx,y:cy+Math.sin(a)*ry})})}
 if(mobile){gd.nodes.slice(1).forEach((n,i)=>positions.set(n.id,{x:i%2?285:95,y:145+Math.floor(i/2)*88}))}else{arrange(first,wide?360:450,wide?250:295,0);arrange(second,640,465,.15)}
 const defs=Object.entries(data.relationTypes).map(([id,t])=>`<marker id="arrow-${id}" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="5" markerHeight="5" orient="auto-start-reverse"><path d="M0 0 10 5 0 10z" fill="${t.color}"/></marker>`).join('');
 const lines=gd.edges.map((e,i)=>{const a=positions.get(e.from),b=positions.get(e.to);const dx=b.x-a.x,dy=b.y-a.y;const scaleA=Math.min(80/(Math.abs(dx)||1),32/(Math.abs(dy)||1)),scaleB=Math.min(80/(Math.abs(dx)||1),32/(Math.abs(dy)||1));const x1=a.x+dx*scaleA,y1=a.y+dy*scaleA,x2=b.x-dx*scaleB,y2=b.y-dy*scaleB;const t=data.relationTypes[e.type];return `<path class="edge" d="M${x1} ${y1}L${x2} ${y2}" fill="none" stroke="${t.color}" stroke-width="${e.type==='hierarchy'?1.25:1.6}" stroke-dasharray="${t.dash}" opacity="${e.type==='hierarchy'?.45:.65}" marker-end="url(#arrow-${e.type})"><title>${esc(byId.get(e.from).label)} → ${t.label.toLowerCase()} → ${esc(byId.get(e.to).label)}. ${esc(e.note)}</title></path>`}).join('');
 const nodeEls=gd.nodes.map(n=>{const p=positions.get(n.id);const selected=n.id===state.selected;const lines=wrap(n.label,mobile?12:13);return `<g class="graph-node" role="button" tabindex="0" data-action="navigate-graph" data-id="${n.id}" aria-label="Recentrer sur ${esc(n.label)}" transform="translate(${p.x-80} ${p.y-32})"><title>${esc(n.summary)}</title><rect width="160" height="64" rx="12" fill="${selected?'#25312e':'white'}" stroke="${selected?'#25312e':n.color}" stroke-width="${selected?2:1}"/><circle cx="15" cy="16" r="3.5" fill="${n.color}"/><text x="25" y="19" fill="${selected?'#cad4cc':'#7c867b'}" font-size="8.5" letter-spacing=".6">${esc(n.kind.toUpperCase())}</text>${lines.map((l,i)=>`<text x="14" y="${36+i*14}" fill="${selected?'#fff':'#25312e'}" font-size="${mobile?12:13}" font-weight="500">${esc(l)}</text>`).join('')}</g>`}).join('');
 return `<div class="relation-filters" aria-label="Types de relations">${Object.entries(data.relationTypes).map(([id,t])=>`<label style="--relation:${t.color}" title="${esc(t.description)}"><input type="checkbox" data-relation="${id}" ${state.enabled.has(id)?'checked':''}><span class="line-key ${t.dash?'dashed':''}"></span>${esc(t.label)}</label>`).join('')}</div><div class="graph-wrap ${mobile?'mobile':''}"><div class="graph-toolbar"><small>${gd.nodes.length} nœuds · ${gd.edges.length} relations${gd.omitted?` · ${gd.omitted} voisins supplémentaires dans l’index`:''}</small><button class="btn" data-action="depth" aria-pressed="${state.depth===2}">${icon('layers')}${state.depth===1?'Voisins directs':'Deux niveaux'}</button><div class="zoom-control"><button data-action="zoom-out" aria-label="Réduire le zoom">${icon('minus')}</button><span id="zoom-label">${Math.round(state.zoom*100)}%</span><button data-action="zoom-in" aria-label="Augmenter le zoom">${icon('plus')}</button></div><button class="btn" data-action="fit" aria-label="Recentrer et ajuster le graphe">${icon('fit')}Ajuster</button><button class="btn" data-action="details" data-id="${state.selected}">${icon('book')}Fiche</button></div><div class="graph-viewport" id="graph-viewport"><svg class="graph-svg" id="graph-svg" viewBox="0 0 ${w} ${h}" data-cx="${cx}" data-cy="${cy}" style="${mobile?`aspect-ratio:${w}/${h}`:''}" role="group" aria-label="Relations autour de ${esc(byId.get(state.selected).label)}"><defs>${defs}</defs><g id="graph-transform" transform="translate(${state.pan.x} ${state.pan.y}) translate(${cx} ${cy}) scale(${state.zoom}) translate(${-cx} ${-cy})"><ellipse cx="${cx}" cy="${cy}" rx="${wide?360:450}" ry="${wide?250:295}" fill="none" stroke="#e2e5db" stroke-width=".8" stroke-dasharray="3 6"/>${wide?`<ellipse cx="${cx}" cy="${cy}" rx="640" ry="465" fill="none" stroke="#e2e5db" stroke-dasharray="3 6"/>`:''}${lines}${nodeEls}</g></svg></div><div class="graph-note">Flèches : sens de la relation. Couleur et pointillés : type de lien. ${mobile?'Faites défiler pour explorer les voisins. Les boutons contrôlent le zoom.':'Glissez pour déplacer, zoomez avec les boutons ou la molette.'}${gd.omitted?' Le graphe limite volontairement les voisins affichés pour préserver la lisibilité.':''}${!matches(byId.get(state.selected))?' Le concept central reste affiché comme contexte malgré les filtres.':''}</div></div><details class="relations-detail" ${gd.omitted?'open':''}><summary>Toutes les relations directes (${gd.allDirect.length}) · liste accessible et navigable</summary><div class="relation-list">${gd.allDirect.map(e=>relationRow(e,state.selected)).join('')||'<p style="font-size:11px;color:var(--muted)">Aucun lien de ce type. Activez d’autres types de relations.</p>'}</div></details>`}
let graphMeasure;
function wrap(s,fontSize){
 if(!graphMeasure)graphMeasure=document.createElement('canvas').getContext('2d');
 graphMeasure.font=`500 ${fontSize}px -apple-system, BlinkMacSystemFont, Segoe UI, sans-serif`;
 const words=s.split(' '),lines=[''];
 for(const word of words){const i=lines.length-1,candidate=(lines[i]+' '+word).trim();if(graphMeasure.measureText(candidate).width>132&&lines[i])lines.push(word);else lines[i]=candidate}
 if(lines.length>2){let last=lines.slice(1).join(' ');while(last&&graphMeasure.measureText(last+'…').width>132)last=last.slice(0,-1);return [lines[0],last+'…']}
 return lines;
}
function relationRow(e,id){const t=data.relationTypes[e.type];const other=e.from===id?e.to:e.from;return `<button class="relation-row" data-action="navigate-graph" data-id="${other}" style="--relation:${t.color}" title="${esc(e.note)}"><span>${esc(byId.get(e.from).label)}</span><span class="rel-verb">${esc(t.label)}</span>${icon('arrow')}<span>${esc(byId.get(e.to).label)}</span></button>`}
function renderDrawer(){const n=byId.get(state.drawer);const related=relevantEdges(n.id);return `<div class="drawer-backdrop"></div><aside class="drawer" role="dialog" aria-modal="true" aria-labelledby="drawer-title" style="--node:${n.color}"><div class="drawer-head">Fiche synthétique<button id="drawer-close" class="icon-btn" data-action="drawer-close" aria-label="Fermer la fiche">${icon('close')}</button></div><div class="drawer-scroll"><div class="drawer-title-row"><div class="domain-icon">${icon(domainIcons[n.domain])}</div><button class="icon-btn ${state.favorites.has(n.id)?'saved':''}" data-action="bookmark" data-id="${n.id}" aria-label="${state.favorites.has(n.id)?'Retirer des':'Ajouter aux'} repères" aria-pressed="${state.favorites.has(n.id)}">${icon('bookmark')}</button></div><div class="kind">${esc(n.kind)} · Niveau ${n.level}</div><h2 id="drawer-title">${esc(n.label)}</h2><div class="drawer-path">${ancestors(n.id).slice(1).map(id=>esc(byId.get(id).label)).join(' › ')}</div><p>${esc(n.summary)}</p>${n.tags.length?`<div class="drawer-tags">${n.tags.map(x=>`<span class="mini-tag">${esc(x)}</span>`).join('')}</div>`:''}<section class="drawer-section"><h3>${icon('compass')}À quoi cela sert</h3><p>${esc(n.use)}</p></section><section class="drawer-section caution-box"><h3>${icon('warning')}Point d’attention</h3><p>${esc(n.caution)}</p></section><section class="drawer-section"><h3>${icon('book')}Références</h3>${n.sources.map(id=>sourceLink(data.sources[id])).join('')}<p style="font-size:8px;margin-top:10px">Synthèse originale. Références consultées le 15 septembre 2026.</p></section><section class="drawer-section"><h3>${icon('network')}Connexions (${related.length})</h3>${related.map(e=>{const other=e.from===n.id?e.to:e.from;return `<button class="drawer-relation" data-action="drawer-related" data-id="${other}" title="${esc(e.note)}"><span class="tree-dot" style="--node:${byId.get(other).color}"></span><span>${esc(byId.get(other).label)}<small>${e.from===n.id?`Ce concept ${data.relationTypes[e.type].label.toLowerCase()}`:`${data.relationTypes[e.type].label} ce concept`} · ${esc(e.note)}</small></span>${icon('chevron')}</button>`}).join('')}</section></div><div class="drawer-actions"><button class="btn" data-action="copy" data-id="${n.id}">${icon('link')}Copier le lien</button><button class="btn primary" data-action="navigate-graph" data-id="${n.id}">Explorer les liens ${icon('arrow')}</button></div></aside>`}
function sourceLink(s){return `<a class="source-link" href="${esc(s.url)}" target="_blank" rel="noopener noreferrer">${icon('external')}<strong>${esc(s.title)}</strong><small>${esc(s.publisher)} · ${esc(s.type)}</small></a>`}
function openDrawer(id){drawerReturn=document.activeElement;state.drawer=id;render();announce(`Fiche ${byId.get(id).label}`)}
function closeDrawer(){state.drawer=null;render();if(drawerReturn?.dataset?.id){const el=[...app.querySelectorAll('[data-action="details"]')].find(x=>x.dataset.id===drawerReturn.dataset.id);el?.focus({preventScroll:true})}else document.getElementById('main-title')?.focus({preventScroll:true})}
function saveBookmark(id){state.favorites.has(id)?state.favorites.delete(id):state.favorites.add(id);let stored=true;try{localStorage.setItem('atlas-ia-favorites-v1',JSON.stringify([...state.favorites]))}catch{stored=false}render();toast(stored?(state.favorites.has(id)?'Concept ajouté à vos repères.':'Concept retiré des repères.'):'Repères conservés pour cette session uniquement.');app.querySelector('[data-action="bookmark"]')?.focus({preventScroll:true})}
function modal(type){const el=document.getElementById('modal');let title='',body='';if(type==='sources'){title='Des repères, avec des références.';body=`<p>${Object.keys(data.sources).length} sources académiques, documentations officielles et référentiels. Consultez les références propres à chaque concept dans sa fiche. Une source fondatrice ancienne reste pertinente pour un concept stable.</p><div class="source-catalog">${Object.values(data.sources).map(s=>`<a class="catalog-source" href="${esc(s.url)}" target="_blank" rel="noopener noreferrer"><strong>${esc(s.publisher)} ${icon('external')}</strong><span>${esc(s.title)}</span><small>${esc(s.type)} · consulté le 15/09/2026</small></a>`).join('')}</div>`}else if(type==='legend'){title='Comment lire les connexions';body=`<p>Les flèches vont du sujet vers l’objet. Les domaines colorent les concepts ; la couleur des traits indique la nature de la relation.</p>${Object.values(data.relationTypes).map(t=>`<div class="legend-row" style="--relation:${t.color}"><span class="line-key ${t.dash?'dashed':''}"></span><div><strong>${esc(t.label)}</strong><p>${esc(t.description)}</p></div></div>`).join('')}<h3>Les niveaux</h3><p>Niveau 1 : grands domaines. Niveau 2 : familles. Niveaux 3 et 4 : concepts, méthodes et technologies. La forme du graphe sert à la navigation et ne représente pas une distance scientifique.</p><h3>Les domaines</h3><div class="drawer-tags">${domains.map(n=>`<span class="mini-tag" style="border-left:3px solid ${n.color}">${esc(n.label)}</span>`).join('')}</div>`}else if(type==='methodology'){title='Périmètre & méthode';body=`<p>${esc(data.methodology)}</p><h3>Une carte de concepts</h3><p>Les dix domaines servent de points d’entrée. Chaque concept a un parent principal pour la navigation et peut être relié à plusieurs domaines. L’IA générative, les agents et la multimodalité recoupent donc les méthodes d’apprentissage.</p><h3>Une sélection détaillée, pas exhaustive</h3><p>La carte couvre ${data.nodes.length-1} concepts, ${data.edges.length} liens et ${Object.keys(data.sources).length} références. Elle privilégie les notions stables et les patterns d’architecture. Elle ne classe pas les fournisseurs, ne recommande pas de produits et n’est pas un inventaire de tous les modèles disponibles.</p><h3>Relations interprétées</h3><p>Les liens sont des synthèses pédagogiques, y compris quand ils sont étayés par une documentation technique. « Utilise » signifie « peut utiliser ». « Soutient » exprime une contribution possible, sans garantie. Les relations détaillées portent leur contexte et leurs références dans les données.</p><h3>Vos repères</h3><p>Les signets sont enregistrés dans votre navigateur sur cet appareil. Aucun compte supplémentaire, formulaire ou service de suivi n’est utilisé par cette application.</p><h3>Actualisation</h3><p>Références consultées le 15 septembre 2026. La carte est une version datée ; elle ne se met pas automatiquement à jour.</p>`}else{title='Un atlas à explorer à votre rythme';body=`<p>Commencez par un domaine, descendez dans les familles, puis ouvrez une fiche. Passez au graphe pour traverser les domaines et comprendre les dépendances possibles.</p><h3>Recherche & filtres</h3><p>La recherche est globale : noms, acronymes, synonymes, définitions et usages. Les filtres se combinent. Sans recherche, l’arborescence montre les enfants du concept actuel ; l’index montre tous ses descendants.</p><h3>Le graphe</h3><p>Cliquez sur un nœud pour le mettre au centre. Activez les types de liens, passez à deux niveaux ou utilisez le zoom. Tous les liens directs restent disponibles dans la liste navigable sous le graphe, même lorsque le nombre de nœuds affichés est limité.</p><h3>Repères & liens</h3><p>Le signet d’une fiche ajoute le concept à « Mes repères ». « Copier le lien » partage le concept et la vue sur cette cartographie publique.</p><div class="help-grid"><kbd>/</kbd><span>Placer le curseur dans la recherche</span><kbd>Échap</kbd><span>Fermer la fiche, l’aide ou la navigation</span><kbd>Tab</kbd><span>Parcourir les contrôles et les nœuds</span><kbd>Entrée</kbd><span>Activer le contrôle ou le nœud sélectionné</span><kbd>← / →</kbd><span>Changer de vue depuis les onglets</span></div>`}
el.innerHTML=`<div class="modal-head"><h2 id="modal-title">${title}</h2><button class="icon-btn" data-action="modal-close" aria-label="Fermer la fenêtre">${icon('close')}</button></div><div class="modal-body">${body}</div>`;el.showModal()}
document.addEventListener('click',async event=>{const el=event.target.closest('[data-action]');if(!el)return;const {action,id,view,step}=el.dataset;switch(action){case'navigate':navigate(id);break;case'navigate-graph':navigate(id,'graph');break;case'expand':state.expanded.has(id)?state.expanded.delete(id):state.expanded.add(id);render();[...app.querySelectorAll('[data-action="expand"]')].find(x=>x.dataset.id===id)?.focus({preventScroll:true});break;case'view':state.view=view;state.page=1;state.zoom=1;state.pan={x:0,y:0};updateRoute();render();document.getElementById('tab-'+view).focus({preventScroll:true});break;case'details':openDrawer(id);break;case'drawer-close':closeDrawer();break;case'drawer-related':state.drawer=id;render();break;case'bookmark':saveBookmark(id);break;case'copy':{const url=new URL(location.href);url.hash=new URLSearchParams({concept:id,view:'graph'}).toString();try{await navigator.clipboard.writeText(url.href);toast('Lien du concept copié.')}catch{modalCopy(url.href)}break;}case'sources':case'legend':case'help':case'methodology':modal(action);break;case'modal-close':document.getElementById('modal').close();break;case'favorites':state.savedOnly=true;state.q='';state.kind='';state.domain='';state.level='';state.page=1;state.menu=false;state.view='index';state.selected='ai';updateRoute();render();announce(`${state.favorites.size} repères`);break;case'reset':clearTimeout(searchTimer);state.q='';state.kind='';state.domain='';state.level='';state.savedOnly=false;state.page=1;render();announce('Filtres réinitialisés');break;case'clear-search':clearTimeout(searchTimer);state.q='';state.page=1;render();document.getElementById('search').focus();break;case'menu':state.menu=!state.menu;render();if(state.menu)app.querySelector('.sidebar-nav').focus();break;case'menu-close':state.menu=false;render();app.querySelector('.mobile-menu').focus();break;case'depth':state.depth=state.depth===1?2:1;state.pan={x:0,y:0};state.zoom=1;render();app.querySelector('[data-action="depth"]').focus({preventScroll:true});break;case'zoom-in':zoom(state.zoom*1.2);break;case'zoom-out':zoom(state.zoom/1.2);break;case'fit':state.pan={x:0,y:0};state.zoom=1;applyTransform();break;case'page':state.page+=Number(step);render();document.querySelector('.workspace')?.scrollIntoView({block:'start',behavior:'smooth'});break;}});
function modalCopy(url){const el=document.getElementById('modal');el.innerHTML=`<div class="modal-head"><h2 id="modal-title">Lien du concept</h2><button class="icon-btn" data-action="modal-close" aria-label="Fermer">${icon('close')}</button></div><div class="modal-body"><p>Copiez ce lien pour retrouver le concept. Cette cartographie est publique.</p><input id="copy-url" readonly value="${esc(url)}" aria-label="Lien à copier" style="width:100%;padding:12px;border:1px solid var(--line);border-radius:6px;font-size:11px"></div>`;el.showModal();const input=document.getElementById('copy-url');input.focus();input.select()}
document.addEventListener('input',event=>{if(event.target.id==='search'){clearTimeout(searchTimer);const value=event.target.value;searchTimer=setTimeout(()=>{state.q=value;state.page=1;render();announce(`${filteredNodes().length} résultats`)},100)}});
document.addEventListener('change',event=>{const filter=event.target.dataset.filter,relation=event.target.dataset.relation;if(filter){state[filter]=event.target.value;state.page=1;render();announce(`${filteredNodes().length} résultats`)}if(relation){event.target.checked?state.enabled.add(relation):state.enabled.delete(relation);render();app.querySelector(`[data-relation="${relation}"]`)?.focus({preventScroll:true});announce('Relations mises à jour')}});
window.addEventListener('popstate',()=>{const p=new URLSearchParams(location.hash.slice(1));if(byId.has(p.get('concept')))state.selected=p.get('concept');state.view=['tree','graph','index'].includes(p.get('view'))?p.get('view'):'tree';state.drawer=null;state.q='';state.kind='';state.domain='';state.level='';state.savedOnly=false;state.page=1;state.zoom=1;state.pan={x:0,y:0};for(const id of ancestors(state.selected))state.expanded.add(id);render()});
document.addEventListener('keydown',event=>{const target=event.target;const editing=['INPUT','TEXTAREA','SELECT'].includes(target.tagName);if(event.key==='/'&&!editing&&!document.getElementById('modal').open&&!state.drawer){event.preventDefault();document.getElementById('search').focus()}if(event.key==='Escape'){if(state.drawer){event.preventDefault();closeDrawer()}else if(state.menu){state.menu=false;render();app.querySelector('.mobile-menu').focus()}else if(editing&&state.q){state.q='';render()}}if((event.key==='Enter'||event.key===' ')&&target.classList.contains('graph-node')){event.preventDefault();navigate(target.dataset.id,'graph')}if(target.getAttribute('role')==='tab'&&['ArrowRight','ArrowLeft','Home','End'].includes(event.key)){event.preventDefault();const views=['tree','graph','index'];let idx=views.indexOf(state.view);idx=event.key==='Home'?0:event.key==='End'?2:(idx+(event.key==='ArrowRight'?1:2))%3;state.view=views[idx];state.page=1;updateRoute();render();document.getElementById('tab-'+state.view).focus()}if(event.key==='Tab'&&state.drawer){const focusables=[...app.querySelectorAll('.drawer button,.drawer a')].filter(x=>!x.disabled),first=focusables[0],last=focusables.at(-1);if(event.shiftKey&&target===first){event.preventDefault();last.focus()}else if(!event.shiftKey&&target===last){event.preventDefault();first.focus()}}if(event.key==='Tab'&&state.menu){const focusables=[...app.querySelectorAll('.sidebar button')],first=focusables[0],last=focusables.at(-1);if(event.shiftKey&&target===first){event.preventDefault();last.focus()}else if(!event.shiftKey&&target===last){event.preventDefault();first.focus()}}});
function zoom(value){state.zoom=Math.max(.4,Math.min(2.5,value));applyTransform()}
function applyTransform(){const el=document.getElementById('graph-transform');if(!el)return;const svg=document.getElementById('graph-svg'),cx=Number(svg.dataset.cx),cy=Number(svg.dataset.cy);el.setAttribute('transform',`translate(${state.pan.x} ${state.pan.y}) translate(${cx} ${cy}) scale(${state.zoom}) translate(${-cx} ${-cy})`);document.getElementById('zoom-label').textContent=Math.round(state.zoom*100)+'%'}
let drag=null;
document.addEventListener('pointerdown',event=>{const viewport=event.target.closest('#graph-viewport');if(!viewport||innerWidth<=600||event.target.closest('.graph-node')||event.button!==0)return;const svg=document.getElementById('graph-svg'),matrix=svg.getScreenCTM();drag={x:event.clientX,y:event.clientY,pan:{...state.pan},sx:1/matrix.a,sy:1/matrix.d,id:event.pointerId};viewport.setPointerCapture(event.pointerId);viewport.classList.add('dragging')});
document.addEventListener('pointermove',event=>{if(!drag||event.pointerId!==drag.id)return;state.pan={x:drag.pan.x+(event.clientX-drag.x)*drag.sx,y:drag.pan.y+(event.clientY-drag.y)*drag.sy};applyTransform()});
document.addEventListener('pointerup',()=>{drag=null;document.getElementById('graph-viewport')?.classList.remove('dragging')});document.addEventListener('pointercancel',()=>{drag=null;document.getElementById('graph-viewport')?.classList.remove('dragging')});
document.addEventListener('wheel',event=>{if(!event.target.closest('#graph-viewport')||innerWidth<=600)return;event.preventDefault();zoom(state.zoom*(event.deltaY<0?1.08:1/1.08))},{passive:false});
// Expose immutable content for ergonomic and integrity checks, not application state.
window.atlasReady=()=>Boolean(data&&byId&&document.getElementById('main'));
